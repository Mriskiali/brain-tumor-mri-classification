# model/

Letakkan model terlatih di sini:

- `hybrid_autoencoder_cnn.h5` — weights hasil training (upload via GitHub Releases jika > 25 MB, lalu taruh link di README)

Direkomendasikan juga:
- `training_history.png` — grafik akurasi/loss
- `confusion_matrix.png` — hasil evaluasi test set
