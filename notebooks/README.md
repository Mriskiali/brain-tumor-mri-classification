# notebooks/

Letakkan notebook training di sini, misal:

- `brain_tumor_classification.ipynb` — notebook Google Colab: load dataset, preprocessing CLAHE, training hybrid Autoencoder + CNN, evaluasi, export model .h5

Tips: sebelum push, jalankan "Edit > Clear all outputs" di Colab agar file ringan (atau push versi dengan outputs kalau mau hasil terlihat langsung di GitHub).
